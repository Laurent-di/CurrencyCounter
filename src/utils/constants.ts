// Numeric input validation regex
export const NUMERIC_REGEX = /^\d*\.?\d*$/;

// Debounce delay for API calls (in milliseconds)
export const DEBOUNCE_DELAY = 500;

// API key error identifier
export const API_KEY_ERROR = 'VITE_API_KEY';

// Number formatting constants
export const DECIMAL_PLACES = {
  DISPLAY: 4,
  AMOUNT: 2,
} as const;

